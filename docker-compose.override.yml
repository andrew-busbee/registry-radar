# Docker Compose Override for Local Development
# This file is automatically used by docker-compose for local development
# It overrides the production image with a local build

services:
  registry-radar:
    build: .
    # Override the image with local build
    image: registry-radar-test:latest
    pull_policy: never
    environment:
      - NODE_ENV=development
      - PORT=3001
